# Docs Site

Built with [Docusaurus](https://docusaurus.io/).

```bash
cd docs-site
npm install
npm start        # dev server on localhost:3000
npm run build    # production build → build/
```

Deployed to GitHub Pages automatically on push to `main` via the `deploy-docs.yml` workflow.
